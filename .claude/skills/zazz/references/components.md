# Zazz component API index

The stable **API surface** only — selector, `data-*` attributes and their accepted values,
what powers the component, and where the canonical example lives. **No example markup is
copied here** (that would duplicate the single source). To author a component, fetch the real
markup from the docs page or read `packages/ui/src/ui/{name}/*.html` in-repo, then adapt it.

Variants/sizes follow the shadcn-style pattern: a `data-*` attribute reassigns the
component's local tokens (see `packages/ui/src/ui/button/button.css`). The **default** variant is the
_absence_ of the attribute.

| Component       | Selector(s)                                                                                                                                                                                                         | `data-*` (accepted values)                                                                                                                                                                                        | Powered by                                                                                                                                     | Docs                               |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| Button          | `.ui-button`                                                                                                                                                                                                        | `data-variant`: primary, muted, ghost, destructive, link · `data-size`: sm, icon, icon-sm                                                                                                                         | CSS only                                                                                                                                       | `/docs/components/button`          |
| Button group    | `.ui-button-group` (wraps `.ui-button` children)                                                                                                                                                                    | `data-orientation`: vertical (horizontal is default)                                                                                                                                                              | CSS only — collapses shared borders + inner radii                                                                                              | `/docs/components/button-group`    |
| Toggle          | `.ui-toggle` (label wraps `input[type=checkbox/radio]`)                                                                                                                                                             | `data-variant`: primary, muted, ghost, destructive · `data-size`: sm, icon, icon-sm                                                                                                                               | CSS only — `:has()` drives state; checked = button active look                                                                                 | `/docs/components/toggle`          |
| Toggle group    | `.ui-toggle-group` (wraps `.ui-toggle` children)                                                                                                                                                                    | `data-orientation`: vertical (horizontal is default)                                                                                                                                                              | CSS only — single-select radios / multi-select checkboxes                                                                                      | `/docs/components/toggle-group`    |
| Badge           | `.ui-badge`                                                                                                                                                                                                         | `data-variant`: primary, muted, ghost, link · `data-size`: icon                                                                                                                                                   | CSS only                                                                                                                                       | `/docs/components/badge`           |
| Tooltip         | `.ui-tooltip` › `[data-slot="tooltip-content"][popover="hint"]` (+ `[data-slot="tooltip-arrow"]`)                                                                                                                   | `data-side`: top, bottom, left, right · `data-align`: start, center, end                                                                                                                                          | Interest Invokers (`interestfor`) + Popover + anchor positioning · polyfills: invokers, popover                                                | `/docs/components/tooltip`         |
| Dialog          | `<dialog class="ui-dialog">` › `[data-slot="dialog-content"]/__header/__body/__footer`                                                                                                                              | `data-size`: article, container, screen                                                                                                                                                                           | Invoker Commands (`command="show-modal"`/`"close"`, `commandfor`) + native `<dialog>` + `closedby` · polyfill: invokers                        | `/docs/components/dialog`          |
| Dropdown        | `.ui-dropdown` › `[data-slot="dropdown-popover"][popover="auto"]`; trigger `popovertarget`                                                                                                                          | `data-side` · `data-align`: start, center, end                                                                                                                                                                    | Popover API + anchor positioning · polyfill: popover                                                                                           | `/docs/components/dropdown`        |
| Toaster         | `<ui-toaster class="ui-toaster" popover="manual">` › `[data-slot="toaster-toast"]/__icon/__content/__title/__description/__action/__close` (toasts built by JS)                                                     | `data-position` on region: top-start … bottom-end · trigger: `command="--toast[-variant]"` + `commandfor` + `data-title/description/variant/duration` · toast `data-variant`: success, info, warning, destructive | **Zazz JS** (`toaster.js`) + Popover (`manual`) + custom Invoker Command `--toast`; imperative `window.Toaster` · polyfills: popover, invokers | `/docs/components/toaster`         |
| Navigation menu | `.ui-navigation-menu` › `[data-slot="navigation-menu-popover"]` (+ `__list`, `__item`, `__trigger`, `__viewport`, `__link`, `__submenu`, `__submenu-trigger`); triggers `popovertarget`                             | `data-align`: center, end · `data-variant`: submenu · `data-size`: root, container, screen · `data-animation`: slide-down                                                                                         | Popover API + anchor positioning                                                                                                               | `/docs/components/navigation-menu` |
| Accordion       | `.ui-accordion` › native `<details>`/`<summary>`                                                                                                                                                                    | —                                                                                                                                                                                                                 | Native disclosure (`::details-content` transition)                                                                                             | `/docs/components/accordion`       |
| Tabs            | `<ui-tabs class="ui-tabs">` › `[data-slot="tabs-list"][role=tablist]`, `[data-slot="tabs-indicator"]`, `[data-slot="tabs-label"]`, grouped `input[type=radio][role=tab]`, `[data-slot="tabs-panel"][role=tabpanel]` | `data-orientation`: vertical (horizontal is default)                                                                                                                                                              | CSS-first radios + `<ui-tabs>` (`tabs.js`) for orientation-aware arrows, Home/End, wrap-around                                                 | `/docs/components/tabs`            |
| Card            | utility composition: `bg-card text-card-foreground border rounded-lg overflow-clip` + figure/badge/`text-*` (no `.card` class)                                                                                      | —                                                                                                                                                                                                                 | **Composition pattern** — pure utilities (`--card` surface, `--border`, `--radius-lg`)                                                         | `/docs/components/card`            |
| Avatar          | utility composition: `grid grid-area-pile size-* border rounded-full overflow-clip` › `<img>` + text fallback                                                                                                       | —                                                                                                                                                                                                                 | **Composition pattern** — no `.avatar` class; pure utilities                                                                                   | `/docs/components/avatar`          |
| Prose           | `.ui-prose` (in `_typography.css`)                                                                                                                                                                                  | —                                                                                                                                                                                                                 | Long-form content wrapper — styles headings/paragraphs/lists/figures                                                                           | `/docs/components/prose`           |
| Table           | `.ui-table` on a semantic `<table>` (caption/thead/tbody/tfoot/tr/th/td)                                                                                                                                            | `data-variant`: alternating, grid · `data-size`: sm · `data-layout`: fixed · row `data-state`: selected · caption `data-side`: top                                                                                | CSS only — every setting is a `--table-*` token                                                                                                | `/docs/components/table`           |
| Lightbox        | `<ui-lightbox class="ui-lightbox">` › `__gallery`, `<ui-carousel data-slot="lightbox-stage">`, `__slide/__thumbs/__thumb`, `<dialog>`, dialog `<ui-carousel>`                                                       | Embla hooks/options (see `apis.md`) · `command="show-modal"`/`commandfor` · `command="close"`                                                                                                                     | **Zazz JS** (`carousel.js`, `lightbox.js`, `embla.js` + Embla CDN) + Dialog + Invoker Commands                                                 | `/docs/components/lightbox`        |
| Slider          | `<input type="range">` (styled in `_reset.css`)                                                                                                                                                                     | —                                                                                                                                                                                                                 | CSS only                                                                                                                                       | `/docs/components/slider`          |
| Switch          | `<input type="checkbox" role="switch">` (`switch.css`)                                                                                                                                                              | —                                                                                                                                                                                                                 | CSS only (restyled native input)                                                                                                               | `/docs/components/switch`          |
| Input           | `.ui-input` (`input.css`, shares `--field-*`)                                                                                                                                                                       | — (type-driven)                                                                                                                                                                                                   | CSS only · validation via `:user-invalid`                                                                                                      | `/docs/components/input`           |
| Input group     | `.ui-input-group` › `[data-slot="input-group-addon"]` + `.ui-input`                                                                                                                                                 | `data-align` on `__addon`: inline-start, inline-end, block-start, block-end                                                                                                                                       | CSS only                                                                                                                                       | `/docs/components/input-group`     |
| Password group  | `<ui-password>` › `.ui-password-group` › `[data-slot="password-group-addon"]` (toggle = `.ui-button[data-variant="ghost"]`)                                                                                         | `data-align` on `__addon` (same values) · optional `data-label-show` / `data-label-hide` on `<ui-password>`                                                                                                       | **Zazz JS** (`password-group.js`) flips password/text + ARIA; CSS swaps icons via `aria-pressed`                                               | `/docs/components/password-group`  |
| Textarea        | `.ui-textarea` (`textarea.css`, `--field-*`)                                                                                                                                                                        | —                                                                                                                                                                                                                 | CSS only · `:user-invalid`                                                                                                                     | `/docs/components/textarea`        |
| Select          | `.ui-select` (`select.css`)                                                                                                                                                                                         | —                                                                                                                                                                                                                 | Native `appearance: base-select`, `@supports`-gated with fallback                                                                              | `/docs/components/select`          |
| Checkbox        | `<input type="checkbox">` (`fields.css`, `_reset.css`)                                                                                                                                                              | —                                                                                                                                                                                                                 | CSS only (restyled native input)                                                                                                               | `/docs/components/checkbox`        |
| Radio           | `.ui-radio` / `.ui-radio-group` (`radio.css`, `--field-*`)                                                                                                                                                          | —                                                                                                                                                                                                                 | CSS only                                                                                                                                       | `/docs/components/radio`           |
| Carousel        | `<ui-carousel>` root + `[data-carousel]` child roles + `.basis-*` slide widths (no `.carousel` class)                                                                                                               | Embla roles + `data-carousel-*` options on `<ui-carousel>` (loop, align, autoplay, autoscroll, classnames)                                                                                                        | **Zazz JS** (`carousel.js`, `embla.js` + Embla CDN); legacy `data-carousel="root"` still works for low-level examples                          | `/docs/components/carousel`        |
| Breadcrumbs     | composition: `<nav>` › `<menu>` › `.ui-button[data-variant="link"]` + muted `/` separators                                                                                                                          | current crumb `aria-disabled="true"`                                                                                                                                                                              | **Composition pattern** — link buttons + utilities                                                                                             | `/docs/components/breadcrumbs`     |
| Mobile menu     | `<dialog class="ui-dialog ui-mobile-menu">` › `[data-slot="mobile-menu-viewport"]/__header/__body/__footer` + nested `.ui-accordion`                                                                                | `data-size`: screen · `data-animation`: slide-right                                                                                                                                                               | Invoker Commands (`command="show-modal"`/`"close"`) + native `<dialog>` + native `<details>` accordions · polyfill: invokers                   | `/docs/components/mobile-menu`     |

## Forms

All field types share `--field-*` tokens (`fields.css`, the Tier-3 owner). Wrap a control in
`.ui-field` (supports `data-orientation="horizontal"`) for the label / control / hint / error
layout. Validation surfaces via `:user-invalid` — **after** blur/submit, never while typing.

## Behaviors (not components — see `apis.md`)

- **`[popover]` (base)** — `data-side`/`data-align` positioning powers tooltip, dropdown, and
  navigation-menu; dark "inverted" popovers come from a container `style()` query.
- **Web components** (`<ui-carousel>`, `<ui-lightbox>`, `<ui-password>`,
  `<ui-tabs>`, `<ui-toaster>`) are light-DOM progressive enhancements; use the real
  examples as the source of truth.
- **Reveal** (`[data-reveal]` / `[data-reveal-each]`) and **Embla** (`[data-carousel]`) are
  data-attribute-driven JS behaviors. Their full attribute catalogs are in `apis.md`.

> Trust this index + the variant selectors in each component's stylesheet
> (`packages/ui/src/ui/{name}/{name}.css`) for the real API, and the real example
> markup (`packages/ui/src/ui/{name}/*.html`) for structure. If an example ever shows an
> attribute with no matching stylesheet selector, the selector wins.
