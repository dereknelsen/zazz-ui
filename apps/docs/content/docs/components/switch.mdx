---
title: Switch
description: Toggle control built from native checkbox with switch role.
---

The switch restyles `<input type="checkbox" role="switch">` into a sliding toggle. Bind with `checked`, `name`, and `value`.

## Default

<Preview src="switch/switch" />
