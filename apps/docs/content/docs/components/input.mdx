---
title: Input
description: A text input on the shared field base with native validation styling.
---

Inputs use the `.input` class and share the `--field-*` token family. Validation surfaces via
`:user-invalid` (after blur or submit, never while typing). Pair with a `.field` wrapper for
the label / hint / error layout.

## Default

<Preview src="input/default" />

## Leading icon

<Preview src="input/icon-leading" />

## Trailing icon

<Preview src="input/icon-trailing" />
