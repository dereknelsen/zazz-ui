---
title: Button
description: Actions and links with data-variant and data-size attributes.
---

Buttons use the `.ui-button` class on `<button>` or `<a>` elements. Choose styles with `data-variant` and dimensions with `data-size`. Omitting `data-variant` applies the default style.

## Variants

<Preview src="button/button" />

## With icons

Add an inline `<svg>` inside the button. Icons are sized automatically:

<Preview src="button/button-icon" />

## Icon-only

Use `data-size="icon"` or `data-size="icon-sm"` for square buttons:

<Preview src="button/button-icon-only" />

## With keyboard shortcuts

Add a `<kbd>` element inside the button for shortcut keys:

```html
<button class="ui-button">Search <kbd>Cmd+K</kbd></button>
```

## API

| Attribute      | Values                                                              |
| -------------- | ------------------------------------------------------------------- |
| `data-variant` | `primary`, `muted`, `ghost`, `destructive`, `link` (omit = default) |
| `data-size`    | `sm`, `icon`, `icon-sm`                                             |

Customize buttons globally by overriding `--ui-button-background`, `--ui-button-radius`, `--ui-button-height`, etc. on `:root`.
