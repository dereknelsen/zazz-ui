---
title: Slider
description: Styled native range input element with keyboard navigation.
---

The slider restyles native `<input type="range">` elements. Configure with standard `min`, `max`, `step`, and `value` attributes.

## Default

<Preview src="slider/default" />
