---
title: Radio
description: Restyled native radio inputs grouped for single-choice selection.
---

Radios apply the `.radio` class to native `<input type="radio">` controls. Group elements by assigning matching `name` attributes inside a `.radio-group` container.

## Default

<Preview src="radio/default" />
