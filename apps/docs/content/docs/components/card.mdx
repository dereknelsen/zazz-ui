---
title: Card
description: Product and content card patterns composed using utility classes.
---

Cards are composed using utility classes. Wrap `bg-card text-card-foreground` in a `rounded-md shadow-sm overflow-clip` container, then structure header, media, text, and footer elements using utilities.

## Default

<Preview src="card/default" />

## Subgrid alignment

Use `grid-rows-*` and `grid-rows-subgrid` to align card headers, content, and actions across columns:

<Preview src="card/subgrid" />
