---
title: Layout
description: Display, positioning, overflow, z-index, aspect-ratio, object-fit, order, and container band utilities.
---

Layout utilities control display mode, positioning, overflow, stacking, and page-level container bands.

## Containers

Place a `.container` as a direct child of `main`, `section`, `header`, `footer`, or `article`. The region becomes a layout grid of width bands, and each direct child of the container is placed into a band. See [Layout and containers](/docs/core-concepts/layout) for how the system works and how it differs from a fixed-width container.

```html
<section>
  <div class="container">
    <h1>Title in the default md band</h1>
    <p>Body copy aligned to the same band.</p>
  </div>
</section>
```

### Container bands

| Band           | Max width                  | Use                                      |
| -------------- | -------------------------- | ---------------------------------------- |
| `xs`           | `--breakpoint-xs` (40rem)  | Narrow column                            |
| `sm`           | `--breakpoint-sm` (48rem)  | Forms, card layouts                      |
| `md` (default) | `--breakpoint-md` (64rem)  | Standard content width                   |
| `lg`           | `--breakpoint-lg` (80rem)  | Wide sections                            |
| `xl`           | `--breakpoint-xl` (96rem)  | Dashboards and galleries                 |
| `full`         | Region width minus gutters | Edge-aligned rows retaining page gutters |
| `bleed`        | 100% viewport/region width | Full-bleed media and color bands         |

### Default band

`data-container` on the container sets the default band for all children:

```html
<section>
  <div class="container" data-container="lg">
    <h2>Wide section</h2>
    <p>Children align to the lg band.</p>
  </div>
</section>
```

### Child band overrides

`data-container` on a direct child overrides that child only:

```html
<section>
  <div class="container">
    <h1>Title in default band</h1>
    <figure data-container="bleed">
      <img src="/wide.jpg" alt="" />
    </figure>
  </div>
</section>
```

### Responsive variants

`@md:container` is a band container from its breakpoint up and a plain block below. `@max-md:container` is the inverse: a container only below the breakpoint, falling back to the element's other classes above it (useful for handing off to a grid).

```html
<section>
  <div class="@max-md:container grid @md:grid-cols-2 gap-lg">...</div>
</section>
```

### Article reading measure

Add `data-variant="article"` to center content constrained by `ch` units:

```html
<article>
  <div class="container" data-variant="article" data-container="md">
    <h1>Reading column</h1>
    <p>Constrained to 65ch line length with gutters.</p>
  </div>
</article>
```

| Value          | Measure |
| -------------- | ------- |
| `xs`           | 45ch    |
| `sm`           | 50ch    |
| `md`           | 65ch    |
| `lg` (default) | 70ch    |
| `xl`           | 75ch    |

## Display

| Class          | Declaration             |
| -------------- | ----------------------- |
| `block`        | `display: block`        |
| `inline-block` | `display: inline-block` |
| `inline`       | `display: inline`       |
| `flex`         | `display: flex`         |
| `inline-flex`  | `display: inline-flex`  |
| `grid`         | `display: grid`         |
| `hidden`       | `display: none`         |

### Visibility

| Class       | Declaration           |
| ----------- | --------------------- |
| `visible`   | `visibility: visible` |
| `invisible` | `visibility: hidden`  |

## Position

<Preview src="utilities/position" />

| Class      | Declaration          |
| ---------- | -------------------- |
| `static`   | `position: static`   |
| `relative` | `position: relative` |
| `absolute` | `position: absolute` |
| `fixed`    | `position: fixed`    |
| `sticky`   | `position: sticky`   |

### Insets and offsets

Offsets use logical inset properties, so they follow the writing mode. `start-*` / `end-*` are equivalent aliases of `left-*` / `right-*`.

| Class                                    | Declaration                             |
| ---------------------------------------- | --------------------------------------- |
| `inset-0`                                | `inset: 0`                              |
| `inset-xs` to `inset-xl`                 | `inset: var(--gap-{size})`              |
| `top-*` (`0`, `xs` to `xl`)              | `inset-block-start: var(--gap-{size})`  |
| `bottom-*` (`0`, `xs` to `xl`)           | `inset-block-end: var(--gap-{size})`    |
| `left-*` / `start-*` (`0`, `xs` to `xl`) | `inset-inline-start: var(--gap-{size})` |
| `right-*` / `end-*` (`0`, `xs` to `xl`)  | `inset-inline-end: var(--gap-{size})`   |

```html
<div class="relative">
  <span class="absolute top-sm right-sm ui-badge" data-variant="primary">New</span>
</div>
```

## Overflow

<Preview src="utilities/overflow" />

| Class              | Declaration                                                    |
| ------------------ | -------------------------------------------------------------- |
| `overflow-visible` | `overflow: visible`                                            |
| `overflow-hidden`  | `overflow: hidden`                                             |
| `overflow-clip`    | `overflow: clip`                                               |
| `overflow-auto`    | `overflow: auto`                                               |
| `overflow-x-auto`  | `overflow-x: auto; overflow-y: clip`                           |
| `overflow-y-auto`  | `overflow-y: auto; overflow-x: clip`                           |
| `scrollbar-none`   | Hides scrollbar display while keeping scrollable functionality |

## Stacking (z-index)

| Class                                  | Declaration          |
| -------------------------------------- | -------------------- |
| `z-isolate`                            | `isolation: isolate` |
| `z-0`                                  | `z-index: 0`         |
| `z-10`, `z-20`, `z-30`, `z-40`, `z-50` | `z-index: 10 ... 50` |

## Aspect ratio

<Preview src="utilities/aspect-ratio" />

| Class               | Ratio      | Typical use         |
| ------------------- | ---------- | ------------------- |
| `aspect-square`     | `1 / 1`    | Avatars, thumbnails |
| `aspect-landscape`  | `3 / 2`    | Photography         |
| `aspect-portrait`   | `2 / 3`    | Portrait media      |
| `aspect-widescreen` | `16 / 9`   | Video               |
| `aspect-univisium`  | `2 / 1`    | Editorial media     |
| `aspect-anamorphic` | `2.39 / 1` | Cinematic video     |

## Object fit

<Preview src="utilities/object-fit" />

| Class               | Declaration              |
| ------------------- | ------------------------ |
| `object-contain`    | `object-fit: contain`    |
| `object-cover`      | `object-fit: cover`      |
| `object-fill`       | `object-fit: fill`       |
| `object-none`       | `object-fit: none`       |
| `object-scale-down` | `object-fit: scale-down` |

## Order

<Preview src="utilities/order" />

| Class         | Declaration    |
| ------------- | -------------- |
| `order-first` | `order: -9999` |
| `order-last`  | `order: 9999`  |
| `order-none`  | `order: 0`     |
