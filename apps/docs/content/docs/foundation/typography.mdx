---
title: Typography
description: Fluid typography scale, font stacks, utility classes, and the .ui-prose long-form container.
---

`_typography.css` implements the typography system from `_variables.css`, providing `.ui-prose` formatting for HTML documents and `.text-*` / `.font-*` utility classes.

## Font stacks

Zazz defines raw stacks and semantic aliases on `:root`:

```css
--font-family-body: var(--font-body);
--font-family-heading: var(--font-body);
```

Point `--font-family-heading` at `--font-heading` to customize heading typography. Utility classes `.font-sans`, `.font-serif`, and `.font-mono` reference the raw font stacks.

## Fluid type scale

Type sizes use fluid `clamp()` calculations that scale between mobile and desktop viewport widths without breakpoint queries. Steps include `--font-size-eyebrow`, body sizes (`xs` to `xl`), heading sizes (`h6` to `h1`), and `--font-size-display`.

Each step specifies matching `--font-weight-*`, `--leading-*`, and `--tracking-*` tokens.

## Typography utility classes

`.text-{step}` applies font size, line height, letter spacing, and font weight together:

```html
<span class="text-display">Launch headline</span>
<p class="text-sm text-muted-foreground">Caption copy</p>
```

`.font-*` sets weight directly (`.font-thin` through `.font-black`), plus semantic shortcuts (`.font-body`, `.font-heading`, `.font-strong`). See [Text and font](/docs/foundation/utilities/text-and-font) and [Text color](/docs/foundation/utilities/text-color).

## `.ui-prose` container

Wrap unclassed HTML (such as Markdown or CMS output) in `.ui-prose` to format headings, paragraphs, lists, blockquotes, and figures automatically:

```html
<article class="ui-prose">
  <h1>Page title</h1>
  <p>Body copy with standard typographic rhythm and measures.</p>
</article>
```

### Prose configuration tokens

| Token                       | Default             | Purpose                           |
| --------------------------- | ------------------- | --------------------------------- |
| `--prose-link-color`        | `var(--primary)`    | Link color inside prose           |
| `--prose-img-radius`        | `var(--radius-md)`  | Corner radius on images and video |
| `--prose-img-border`        | `none`              | Border on media elements          |
| `--prose-figcaption-margin` | `var(--gap-xs) 0 0` | Spacing above figure captions     |

Vertical block rhythm is controlled by `--paragraph-spacing-*` tokens.
